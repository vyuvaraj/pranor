project_name: pranor-notify
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: pranor-notify
    main: .
    binary: pranor-notify
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildDate={{.Date}}

archives:
  - id: pranor-notify
    builds: [pranor-notify]
    name_template: "pranor-notify_{{.Version}}_{{.Os}}_{{.Arch}}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - Merge pull request
      - Merge branch

dockers:
  - image_templates:
      - "ghcr.io/vyuvaraj/pranor-notify:{{.Tag}}"
      - "ghcr.io/vyuvaraj/pranor-notify:latest"
    dockerfile: Dockerfile.goreleaser
    build_flag_templates:
      - "--label=org.opencontainers.image.title=Pranor Notify"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.source=https://github.com/vyuvaraj/Pranor Notify"

brews:
  - name: pranor-notify
    repository:
      owner: vyuvaraj
      name: homebrew-serv
    homepage: "https://github.com/vyuvaraj/Pranor Notify"
    description: "Email delivery and SMTP service from the ServVerse platform"
    license: "Apache-2.0"
    install: |
      bin.install "pranor-notify"
    test: |
      system "#{bin}/pranor-notify", "--version"

scoops:
  - repository:
      owner: vyuvaraj
      name: scoop-serv
    homepage: "https://github.com/vyuvaraj/Pranor Notify"
    description: "Email delivery and SMTP service from the ServVerse platform"
    license: "Apache-2.0"


nfpms:
  - id: pranor-notify-pkg
    package_name: pranor-notify
    builds: [pranor-notify]
    vendor: Yuvaraj
    homepage: https://github.com/vyuvaraj/Pranor Notify
    maintainer: Yuvaraj <vyuvaraj@github.com>
    description: Transactional email delivery service from the ServVerse platform
    license: Apache-2.0
    formats:
      - deb
      - rpm
    bindir: /usr/local/bin
    contents:
      - src: README.md
        dst: /usr/share/doc/pranor-notify/README.md
    scripts:
      postinstall: scripts/postinstall.sh
      preremove: scripts/preremove.sh
release:
  mode: replace
