project_name: pranor-trace
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: pranor-trace
    main: .
    binary: pranor-trace
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildDate={{.Date}}

archives:
  - id: pranor-trace
    builds: [pranor-trace]
    name_template: "pranor-trace_{{.Version}}_{{.Os}}_{{.Arch}}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - Merge pull request
      - Merge branch

dockers:
  - image_templates:
      - "ghcr.io/vyuvaraj/pranor-trace:{{.Tag}}"
      - "ghcr.io/vyuvaraj/pranor-trace:latest"
    dockerfile: Dockerfile.goreleaser
    build_flag_templates:
      - "--label=org.opencontainers.image.title=Pranor Trace"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.source=https://github.com/vyuvaraj/Pranor Trace"

brews:
  - name: pranor-trace
    repository:
      owner: vyuvaraj
      name: homebrew-serv
    homepage: "https://github.com/vyuvaraj/Pranor Trace"
    description: "Distributed tracing and observability service from the ServVerse platform"
    license: "Apache-2.0"
    install: |
      bin.install "pranor-trace"
    test: |
      system "#{bin}/pranor-trace", "--version"

scoops:
  - repository:
      owner: vyuvaraj
      name: scoop-serv
    homepage: "https://github.com/vyuvaraj/Pranor Trace"
    description: "Distributed tracing and observability service from the ServVerse platform"
    license: "Apache-2.0"


nfpms:
  - id: pranor-trace-pkg
    package_name: pranor-trace
    builds: [pranor-trace]
    vendor: Yuvaraj
    homepage: https://github.com/vyuvaraj/Pranor Trace
    maintainer: Yuvaraj <vyuvaraj@github.com>
    description: Distributed tracing and request observability from the ServVerse platform
    license: Apache-2.0
    formats:
      - deb
      - rpm
    bindir: /usr/local/bin
    contents:
      - src: README.md
        dst: /usr/share/doc/pranor-trace/README.md
    scripts:
      postinstall: scripts/postinstall.sh
      preremove: scripts/preremove.sh
release:
  mode: replace
