project_name: pranor-auth
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: pranor-auth
    main: .
    binary: pranor-auth
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildDate={{.Date}}

archives:
  - id: pranor-auth
    builds: [pranor-auth]
    name_template: "pranor-auth_{{.Version}}_{{.Os}}_{{.Arch}}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - Merge pull request
      - Merge branch

dockers:
  - image_templates:
      - "ghcr.io/vyuvaraj/pranor-auth:{{.Tag}}"
      - "ghcr.io/vyuvaraj/pranor-auth:latest"
    dockerfile: Dockerfile.goreleaser
    build_flag_templates:
      - "--label=org.opencontainers.image.title=Pranor Auth"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.source=https://github.com/vyuvaraj/Pranor Auth"

brews:
  - name: pranor-auth
    repository:
      owner: vyuvaraj
      name: homebrew-serv
    homepage: "https://github.com/vyuvaraj/Pranor Auth"
    description: "Authentication and authorization service from the ServVerse platform"
    license: "Apache-2.0"
    install: |
      bin.install "pranor-auth"
    test: |
      system "#{bin}/pranor-auth", "--version"

scoops:
  - repository:
      owner: vyuvaraj
      name: scoop-serv
    homepage: "https://github.com/vyuvaraj/Pranor Auth"
    description: "Authentication and authorization service from the ServVerse platform"
    license: "Apache-2.0"


nfpms:
  - id: pranor-auth-pkg
    package_name: pranor-auth
    builds: [pranor-auth]
    vendor: Yuvaraj
    homepage: https://github.com/vyuvaraj/Pranor Auth
    maintainer: Yuvaraj <vyuvaraj@github.com>
    description: Authentication and authorization service from the ServVerse platform
    license: Apache-2.0
    formats:
      - deb
      - rpm
    bindir: /usr/local/bin
    contents:
      - src: README.md
        dst: /usr/share/doc/pranor-auth/README.md
    scripts:
      postinstall: scripts/postinstall.sh
      preremove: scripts/preremove.sh
release:
  mode: replace
