project_name: pranor-mesh
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: pranor-mesh
    main: .
    binary: pranor-mesh
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildDate={{.Date}}

archives:
  - id: pranor-mesh
    builds: [pranor-mesh]
    name_template: "pranor-mesh_{{.Version}}_{{.Os}}_{{.Arch}}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - Merge pull request
      - Merge branch

dockers:
  - image_templates:
      - "ghcr.io/vyuvaraj/pranor-mesh:{{.Tag}}"
      - "ghcr.io/vyuvaraj/pranor-mesh:latest"
    dockerfile: Dockerfile.goreleaser
    build_flag_templates:
      - "--label=org.opencontainers.image.title=Pranor Mesh"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.source=https://github.com/vyuvaraj/Pranor Mesh"

brews:
  - name: pranor-mesh
    repository:
      owner: vyuvaraj
      name: homebrew-serv
    homepage: "https://github.com/vyuvaraj/Pranor Mesh"
    description: "Service mesh and sidecar proxy from the ServVerse platform"
    license: "Apache-2.0"
    install: |
      bin.install "pranor-mesh"
    test: |
      system "#{bin}/pranor-mesh", "--version"

scoops:
  - repository:
      owner: vyuvaraj
      name: scoop-serv
    homepage: "https://github.com/vyuvaraj/Pranor Mesh"
    description: "Service mesh and sidecar proxy from the ServVerse platform"
    license: "Apache-2.0"


nfpms:
  - id: pranor-mesh-pkg
    package_name: pranor-mesh
    builds: [pranor-mesh]
    vendor: Yuvaraj
    homepage: https://github.com/vyuvaraj/Pranor Mesh
    maintainer: Yuvaraj <vyuvaraj@github.com>
    description: Service mesh and traffic management from the ServVerse platform
    license: Apache-2.0
    formats:
      - deb
      - rpm
    bindir: /usr/local/bin
    contents:
      - src: README.md
        dst: /usr/share/doc/pranor-mesh/README.md
    scripts:
      postinstall: scripts/postinstall.sh
      preremove: scripts/preremove.sh
release:
  mode: replace
