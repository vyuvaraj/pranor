project_name: pranor-flow
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: pranor-flow
    main: .
    binary: pranor-flow
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildDate={{.Date}}

archives:
  - id: pranor-flow
    builds: [pranor-flow]
    name_template: "pranor-flow_{{.Version}}_{{.Os}}_{{.Arch}}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - Merge pull request
      - Merge branch

dockers:
  - image_templates:
      - "ghcr.io/vyuvaraj/pranor-flow:{{.Tag}}"
      - "ghcr.io/vyuvaraj/pranor-flow:latest"
    dockerfile: Dockerfile.goreleaser
    build_flag_templates:
      - "--label=org.opencontainers.image.title=Pranor Flow"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.source=https://github.com/vyuvaraj/Pranor Flow"

brews:
  - name: pranor-flow
    repository:
      owner: vyuvaraj
      name: homebrew-serv
    homepage: "https://github.com/vyuvaraj/Pranor Flow"
    description: "Workflow orchestration and pipeline service from the ServVerse platform"
    license: "Apache-2.0"
    install: |
      bin.install "pranor-flow"
    test: |
      system "#{bin}/pranor-flow", "--version"

scoops:
  - repository:
      owner: vyuvaraj
      name: scoop-serv
    homepage: "https://github.com/vyuvaraj/Pranor Flow"
    description: "Workflow orchestration and pipeline service from the ServVerse platform"
    license: "Apache-2.0"


nfpms:
  - id: pranor-flow-pkg
    package_name: pranor-flow
    builds: [pranor-flow]
    vendor: Yuvaraj
    homepage: https://github.com/vyuvaraj/Pranor Flow
    maintainer: Yuvaraj <vyuvaraj@github.com>
    description: Workflow orchestration engine from the ServVerse platform
    license: Apache-2.0
    formats:
      - deb
      - rpm
    bindir: /usr/local/bin
    contents:
      - src: README.md
        dst: /usr/share/doc/pranor-flow/README.md
    scripts:
      postinstall: scripts/postinstall.sh
      preremove: scripts/preremove.sh
release:
  mode: replace
