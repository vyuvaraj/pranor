project_name: pranor-pulse
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: pranor-pulse
    # Pranor Pulse's main package lives in cmd/pranor-pulse/
    main: ./cmd/pranor-pulse
    binary: pranor-pulse
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildDate={{.Date}}

archives:
  - id: pranor-pulse
    builds: [pranor-pulse]
    name_template: "pranor-pulse_{{.Version}}_{{.Os}}_{{.Arch}}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - Merge pull request
      - Merge branch

dockers:
  - image_templates:
      - "ghcr.io/vyuvaraj/pranor-pulse:{{.Tag}}"
      - "ghcr.io/vyuvaraj/pranor-pulse:latest"
    dockerfile: Dockerfile.goreleaser
    build_flag_templates:
      - "--label=org.opencontainers.image.title=Pranor Pulse"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.source=https://github.com/vyuvaraj/Pranor Pulse"

brews:
  - name: pranor-pulse
    repository:
      owner: vyuvaraj
      name: homebrew-pranor
    homepage: "https://github.com/vyuvaraj/Pranor Pulse"
    description: "Distributed message queue and event streaming service from the Pranor platform"
    license: "Apache-2.0"
    install: |
      bin.install "pranor-pulse"
    test: |
      system "#{bin}/pranor-pulse", "--version"

scoops:
  - repository:
      owner: vyuvaraj
      name: scoop-pranor
    homepage: "https://github.com/vyuvaraj/Pranor Pulse"
    description: "Distributed message queue and event streaming service from the Pranor platform"
    license: "Apache-2.0"


nfpms:
  - id: pranor-pulse-pkg
    package_name: pranor-pulse
    builds: [pranor-pulse]
    vendor: Yuvaraj
    homepage: https://github.com/vyuvaraj/Pranor Pulse
    maintainer: Yuvaraj <vyuvaraj@github.com>
    description: Distributed message queue and event streaming from the Pranor platform
    license: Apache-2.0
    formats:
      - deb
      - rpm
    bindir: /usr/local/bin
    contents:
      - src: README.md
        dst: /usr/share/doc/pranor-pulse/README.md
    scripts:
      postinstall: scripts/postinstall.sh
      preremove: scripts/preremove.sh
release:
  mode: replace
