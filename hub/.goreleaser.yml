project_name: pranor-hub
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: pranor-hub
    main: .
    binary: pranor-hub
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildDate={{.Date}}

archives:
  - id: pranor-hub
    builds: [pranor-hub]
    name_template: "pranor-hub_{{.Version}}_{{.Os}}_{{.Arch}}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - Merge pull request
      - Merge branch

dockers:
  - image_templates:
      - "ghcr.io/vyuvaraj/pranor-hub:{{.Tag}}"
      - "ghcr.io/vyuvaraj/pranor-hub:latest"
    dockerfile: Dockerfile.goreleaser
    build_flag_templates:
      - "--label=org.opencontainers.image.title=Pranor Hub"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.source=https://github.com/vyuvaraj/Pranor Hub"

brews:
  - name: pranor-hub
    repository:
      owner: vyuvaraj
      name: homebrew-pranor
    homepage: "https://github.com/vyuvaraj/Pranor Hub"
    description: "Service discovery and registry from the Pranor platform"
    license: "Apache-2.0"
    install: |
      bin.install "pranor-hub"
    test: |
      system "#{bin}/pranor-hub", "--version"

scoops:
  - repository:
      owner: vyuvaraj
      name: scoop-pranor
    homepage: "https://github.com/vyuvaraj/Pranor Hub"
    description: "Service discovery and registry from the Pranor platform"
    license: "Apache-2.0"


nfpms:
  - id: pranor-hub-pkg
    package_name: pranor-hub
    builds: [pranor-hub]
    vendor: Yuvaraj
    homepage: https://github.com/vyuvaraj/Pranor Hub
    maintainer: Yuvaraj <vyuvaraj@github.com>
    description: Service registry and health monitoring from the Pranor platform
    license: Apache-2.0
    formats:
      - deb
      - rpm
    bindir: /usr/local/bin
    contents:
      - src: README.md
        dst: /usr/share/doc/pranor-hub/README.md
    scripts:
      postinstall: scripts/postinstall.sh
      preremove: scripts/preremove.sh
release:
  mode: replace
